apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama-volume
spec:
  storageClassName: slow
  accessModes:
    - ReadWriteOnce # Access modes for volumes is defined under Persistent Volumes
  resources:
    requests:
      storage: 32Gi # volume size requested
